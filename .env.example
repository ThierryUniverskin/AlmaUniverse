# Supabase Configuration
# Get these from your Supabase project dashboard: Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: For server-side admin operations only (never expose to client)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Remove.bg API (for background removal in photo capture)
# Get your API key from: https://www.remove.bg/dashboard
# NOTE: This is a server-side only key (not exposed to browser)
REMOVE_BG_API_KEY=your-remove-bg-api-key-here
